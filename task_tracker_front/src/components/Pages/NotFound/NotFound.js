import React from 'react';
import "./NotFound.css"

const NotFound = () => {
    return (
        <div className={"notfound_page"}>
            NotFound 404 (ERROR)
        </div>
    );
};

export default NotFound;